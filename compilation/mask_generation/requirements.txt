# SAM2 host model and calibration dependencies.
# qbcompiler and mblt come from the Mobilint compiler environment and are not listed here.
torch
numpy
opencv-python
pycocotools
transformers==4.57.1
# sam2_export_onnx.py imports onnxruntime for constant folding and --verify, both
# on by default. The Mobilint compiler environment ships onnxruntime-gpu, which
# provides the same module; skip this line if that is already installed, because
# having both onnxruntime and onnxruntime-gpu present conflicts.
onnxruntime
